# -*- coding: utf-8 -*-


def value_to_digits(value):
    """
    Splits the given value in to single digits and returns an array of digits
    TODO Tests and checks for value needed
    """
    digits = [str(x) for x in str(value)]
    return digits
